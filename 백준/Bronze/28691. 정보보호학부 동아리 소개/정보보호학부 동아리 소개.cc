#include <bits/stdc++.h>
#define fastio cin.tie(0)->sync_with_stdio(0)
using namespace std;
int main() {
    fastio; char c; cin >> c;
    if (c == 'M') cout << "MatKor";
    if (c == 'W') cout << "WiCys";
    if (c == 'C') cout << "CyKor";
    if (c == 'A') cout << "AlKor";
    if (c == '$') cout << "$clear";
}