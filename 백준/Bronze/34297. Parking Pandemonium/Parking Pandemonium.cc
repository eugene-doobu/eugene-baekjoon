#define fastio cin.tie(0)->sync_with_stdio(0)
#include <iostream>
using namespace std;
int main() {
    fastio;
    int m, k, c, ans;
    cin >> m >> k >> c;
    ans = m * c;
    cout << ans << "\n";
}