#include <bits/stdc++.h>
#define fastio cin.tie(0)->sync_with_stdio(0)
using namespace std;
int main() {
    fastio; string s; int n; cin >> n;
    while (n--) {cin >> s; cout << s.size() << '\n';}
}
